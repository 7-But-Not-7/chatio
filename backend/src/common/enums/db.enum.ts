
export enum Themes {
    LIGHT = "light",
    DARK = "dark",
}

export enum VideoType {
    PROFESSIONAL = "professional",
    CASUAL = "casual",
}

export enum ChatMemberRole {
    ADMIN = "admin",
    MEMBER = "member"
}