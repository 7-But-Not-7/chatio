
export enum Themes {
    LIGHT = "light",
    DARK = "dark",
}

export enum VideoType {
    PROFESSIONAL = "professional",
    CASUAL = "casual",
}

export enum ChatMemberRole {
    ADMIN = "admin",
    MEMBER = "member"
}

export enum FileType {
    IMAGE = "image",
    VIDEO = "video",
    AUDIO = "audio",
    DOCUMENT = "document",
}